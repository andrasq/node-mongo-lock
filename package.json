{
  "name": "mongo-lock",
  "version": "0.8.2",
  "main": "mongolock.js",
  "license": "Apache-2.0",
  "description": "mongodb atomic advisory locks like mysql GET_LOCK()",
  "keywords": ["MongoDB", "advisory", "atomic", "locks", "GET_LOCK"],
  "files": ["mongolock.js", "README.md"],

  "author": "andrasq",
  "repository": { "type": "git", "url": "https://github.com/andrasq/node-mongo-lock" },
  "readme": "see README.md in the repo",

  "scripts": {
    "test": "qnit test.js",
    "test-integration": "qnit test.js test-endtoend.js",
    "coverage": "nyc -r lcov -r text npm test",
    "clean": "rm -rf .nyc_output coverage npm-debug.log"
  }
}
